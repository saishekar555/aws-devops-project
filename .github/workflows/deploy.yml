name: Deploy to EC2

on:
push:
branches:
- main

jobs:
deploy:
runs-on: ubuntu-latest

```
steps:
- name: Checkout Code
  uses: actions/checkout@v4

- name: Deployment Triggered
  run: |
    echo "Deployment pipeline started"
    echo "Future step: SSH to EC2 and deploy application"
```
